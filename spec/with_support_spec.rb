require 'spec_helper'

describe WithSupport do
  it 'has a version number' do
    expect(WithSupport::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
